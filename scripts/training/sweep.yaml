project: hazGAN
program: train.py
method: bayes
name: ablation
metric:
  goal: minimize
  name: chi_rmse

parameters:
  # meta-parameters
  epochs:
    values: [100, 200, 300, 400, 500, 600]
  fields:
    values:
      - ['u10']
      - ['u10', 'tp']
      - ['u10', 'mslp']
  target_weights:
    values:
      - [0., .4, .6]
      - [0., .2, .8]
      - [0., 0., 1.]

  # architecture
  lrelu:
    values:
    min: 0.01
    min: 0.5
  momentum:
    min: 0.1
    max: 0.99
  input_policy:
    values: ['add', 'concat']
  augment_policy:
    values: ['color,translation,cutout', '', 'color', 'translation', 'cutout']
  noise_sd:
    values: [0, 0.01, 0.02]
  embedding_depth:
    values: [16, 32, 64]
  channel_multiplier:
    values: [4, 8, 16]
  latent_dims:
    values: [64, 128, 256]
  generator_width:
    values: [64, 128, 256]
  critic_width:
    values: [64, 128, 256]
  
  # training features
  training_balance:
    values: [1, 2, 5, 10]
  batch_size:
    values: [32, 64, 128]
  lambda_gp:
    values: [1, 5, 10, 15, 20]
  lookahead:
    values: [True, False]

  # optimizer
  beta_1:
    min: 0.1
    max: 0.9
  beta_2:
    min: 0.9
    max: 0.9999
  use_ema:
    values: [True, False]
  ema_momentum:
    min: 0.9
    max: 0.999
  weight_decay:
    values: [0, 0.001]


run_cap: 100
command:
  - ${env}
  - ${interpreter}
  - ${program}
